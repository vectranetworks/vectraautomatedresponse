# Create and populate a dict for each vCenter appliance
HOST = "172.20.1.13"
USER = "admin"
PASSWD = "ED"

# STOP
VCSA_HOSTS = [
    {
        "HOST": HOST,
        "USER": USER,
        "PASS": PASSWD,
    }
]
